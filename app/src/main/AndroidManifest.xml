<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.defiance.confine">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NavigationDrawerActivity"></activity>
        <activity android:name=".DietPlanActivity" />
        <activity android:name=".MentalHealthActivity" />
        <activity android:name=".PreventiveMeasuresActivity" />
        <activity android:name=".CovidCasesActivity" />
        <activity android:name=".DosAndDontsActivity" />
        <activity android:name=".MythBustersActivity" />
        <activity android:name=".SelfQuarantineActivity" />
        <activity android:name=".TipsActivity" />
        <activity android:name=".ResultActivity" />
        <activity android:name=".FormActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-1791340627813209~5191202688" />
    </application>

</manifest>